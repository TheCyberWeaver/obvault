**Relative Strength Index (RSI)** is a momentum indicator that compares recent gains with recent losses.

Range:

- `0` to `100`

Typical interpretation:

- above `70`: potentially overbought
- below `30`: potentially oversold

RSI is mainly a technical-analysis tool, so it is less central than the statement-analysis and valuation notes in this folder.
